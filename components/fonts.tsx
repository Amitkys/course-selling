import { Poppins, Inter} from "next/font/google";

export const poppin = Poppins({
    subsets: ['latin'], 
    weight: '400',
    display: 'swap'
});

export const inter = Inter({
    subsets: ['latin'], 
    weight: '400',
    display: 'swap'
});